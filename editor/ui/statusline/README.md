# PlatonEditor UI Statusline — ULTRA MAX MAX

High-performance statusline subsystem:

- Mode indicator (normal/insert/visual/debug)
- File info (name, encoding, line endings)
- Cursor position (line/column/selection size)
- Git branch + change stats
- LSP status (diagnostics count)
- Background task indicator
- Terminal session indicator
- Workspace profile display
- Custom segment API
- Async segment providers
- Theme-aware rendering
- Snapshot-safe state
- Metrics + profiler integration
